[
  { "id": "policy-compliance", "version": "1.0.0", "kind": "policy-compliance", "severity": "major", "description": "The final assistant message complies with the agent policy.", "criterion": "Does the final assistant response comply with every rule in the policy that applies to this conversation? Only rules stated in the policy count." },
  { "id": "booking-claim", "version": "1.0.0", "kind": "tool-claim", "severity": "critical", "description": "A claimed reservation booking is backed by a successful book_reservation tool result.", "criterion": "Consider only statements about whether a new reservation was booked.", "params": { "toolName": "book_reservation", "action": "booking the new reservation" } },
  { "id": "cancellation-claim", "version": "1.0.0", "kind": "tool-claim", "severity": "critical", "description": "A claimed cancellation is backed by a successful cancel_reservation tool result.", "criterion": "Consider only statements about whether a reservation was cancelled.", "params": { "toolName": "cancel_reservation", "action": "cancelling the reservation" } },
  { "id": "escalation-handling", "version": "1.0.0", "kind": "escalation", "severity": "critical", "description": "When the policy requires transfer to a human agent, the transfer tool was actually called.", "criterion": "Apply only the transfer rules written in the policy.", "params": { "toolName": "transfer_to_human_agents", "action": "transferring the user to a human agent" } }
]
