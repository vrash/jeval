# @jeval/cli

The `jeval` command: `init`, `run --mode fixture|live`, `report`, `compare`, `benchmark`, `labels`. Exit codes with `--ci`: 0 gates met · 1 quality gate failed · 2 incomplete · 3 usage error.

Provisional package name; not yet published. See the repository README.
